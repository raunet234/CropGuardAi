# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

CropGuardAi is an AI-powered crop disease detection and agricultural monitoring application. (Update this section once the project structure is established.)

## Getting Started

This repository is in its initial state. Update this file with the following once the project is scaffolded:

- **Build command**: e.g., `npm run build` or `pip install -e .`
- **Dev server**: e.g., `npm run dev`
- **Test command**: e.g., `npm test` or `pytest`
- **Lint command**: e.g., `npm run lint` or `ruff check .`

## Architecture

Document the high-level architecture here once the codebase is established:

- **Frontend**: (framework, key libraries)
- **Backend**: (framework, API structure)
- **AI/ML**: (model type, inference approach)
- **Data storage**: (database, file storage)
